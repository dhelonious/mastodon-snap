#!/bin/bash -e

. "$SNAP/mastodon.env"
. "$SNAP_CURRENT/server.conf"

check_setup
postgres_waitready
redis_waitready
migrations_waitready
load_sidekiq_tuning_env

mastodon_sidekiq -c $SIDEKIQ_DB_POOL \
    2>&1 | tee -a "$LOG_DIR/sidekiq.log"
