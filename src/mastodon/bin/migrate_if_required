#!/bin/bash -e

. "$SNAP/mastodon.env"

check_setup
postgres_waitready

if [ "$(snapctl get migrations-required)" == true ]; then
    echo Start db migrations
    mastodon_db_migrate
    reset_migrations_required
fi
