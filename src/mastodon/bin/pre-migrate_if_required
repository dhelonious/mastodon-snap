#!/bin/bash -e

. "$SNAP/mastodon.env"

check_setup
postgres_waitready

if [ "$(snapctl get pre-migrations-required)" == true ]; then
    echo Start pre-deployment db migrations
    SKIP_POST_DEPLOYMENT_MIGRATIONS=true mastodon_db_migrate
    reset_pre_migrations_required
fi
