#!/bin/bash -e

. "$SNAP/postgres.env"

mkdir -p "$PG_DATA"

# TODO: The option `--no-data-checksums` should be replaced by `--data-checksums` in the future
exec initdb -D "$PG_DATA" -U postgres -A trust -E UTF8 --locale=C "$@" --no-data-checksums
